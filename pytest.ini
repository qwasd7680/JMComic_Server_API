[pytest]
pythonpath = .
testpaths = tests
addopts = -vv -ra -s --maxfail=1 --durations=10 --timeout=60 --timeout-method=thread
markers =
    integration: tests that require network access to JMComic service
    slow: tests that take a long time (downloads)